część 1
1. Volumen: 
$ docker volume create <nazwa> 
(0.1) 
Sprawdzamy wolumen
(0.2)
$ docker volumen inpect <nazwa volumenu>

2. Podpinamy volumen i urchamiamy dockera: 
$ docker run --interactive --tty --mount source=nazwa volumenu,destination=/store ubuntu 
(0.3)

3. Tworzymy plik w kontenerze w folderze store
$ touch store/<nazwa pliku>
(0.4)

4. Tworzymy i kopiujemy plik z hosta:
$ touch <ścieżka do volumenu>/<nazwa pliku>
$ docker cp <ścieżka pliku> <id kontenera>:<ścieżka volumena> 
(0.5)

5. Sprawdzamy czy pliki są na swoich miejscach:
(0.6)


część 2
1. Uruchamiamy ubuntu 
$ sudo  docker run --interactive --tty --mount source=dyskietka2,destination=/store --publish 8080:80 ubuntu
2. Instalujemy open ssh po wczesniejszym zaakutalizowaniu apt-get
$ apt-get update
$ apt-get -y install openssh-server
(1.1)

3. Próba zmiany portu zdefinowanego w pliku /etc/ssh/sshd_config z domyślego 22
$ <edytor tekstu> /etc/ssh/sshd_config
(1.2)

4. Uruchamiamy usługę SSH z podając ścieżkę do wcześniej utowrzonego katalogu /run/sshd
$ /usr/sbin/sshd -D 
(1.3)

5. Stworz klucze SSH
$ ssh-keygen
(1.4)

6. Przenies klucz publiczny do volumenu
$ docker cp <klucz> <id kontenera>:<ścieżka volumena> 
(1.5)

7. Dodaj jako klucz autoryzowany
$ mkdir .ssh
$ cat <klucz> >> /.ssh/<nazwa klucza>
(1.6)

8. Znajdz ip kontenera 172.17.0.1 
$ ifconfig
(1.7)
9. Zaloguj sie do kontenera
sudo ssh root@172.17.0.3 -p 22 -i ssh-keys


część 3 
(2.1)
(2.2)

nie udało się z pododu niewystarczającego miejscca na dysku, walczyłem i ustawiałem ale nic nie pomogło
(2.3)


