version: "3"
services:
  test:
    build:
      context: .
      dockerfile: dockerfile-test
    depends_on:
      - build
  build:
    image: MyApp
    build:
      context: .
      dockerfile: dockerfile-bldr
