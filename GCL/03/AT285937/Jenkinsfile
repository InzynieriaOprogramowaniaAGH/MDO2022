pipeline {
     environment {
        PATH = "$PATH:/usr/bin"
    }
    agent any
    
    stages {
        stage('Build') {
            steps {
                sh 'docker-compose -f GCL/03/AT285937/docker-compose.yml build --no-cache build'
            }
        }
        
    }
    
}
